{
  "name": "dibs-gulp-rev",
  "version": "2.0.2",
  "description": "Dibs fork of sindresorhus/gulp-rev. .manifest accepts a `base` option for trimming the urls that are written to the manifest map",
  "license": "MIT",
  "repository": "1stdibs/dibs-gulp-rev",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "mocha"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    "gulpplugin",
    "rev",
    "revision",
    "hash",
    "optimize",
    "version",
    "versioning",
    "cache",
    "expire",
    "static",
    "asset",
    "assets"
  ],
  "dependencies": {
    "gulp-util": "^3.0.0",
    "object-assign": "^1.0.0",
    "through2": "^0.6.1"
  },
  "devDependencies": {
    "mocha": "*"
  }
}
